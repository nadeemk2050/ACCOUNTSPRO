I have updated the backend code in `functions/index.js` as you requested.

However, I am unable to run the deployment command due to security restrictions.

To complete the fix, please run the following command in your terminal from the `c:\app\accnad-app\functions` directory:

```bash
npm run deploy
```
