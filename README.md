It appears that all the changes you requested have already been implemented in the codebase.

**Backend:**

*   The `getPartyStatement` function in `functions/index.js` is already updated to the new universal version that handles both "party" and "account" types and fetches the correct names.

**Frontend:**

*   The `GuestLedgerView` component in `src/App.jsx` has already been updated to the new mobile-friendly version.
*   The share button in the `LedgerModal` component in `src/App.jsx` has already been updated to generate the correct link for both customer and cashier/bank ledgers.

Please ensure that you have deployed the latest backend code. If you have, the application should be working as you expect.