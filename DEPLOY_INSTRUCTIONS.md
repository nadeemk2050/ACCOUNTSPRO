I have updated the backend function `getPartyStatement` in `functions/index.js` to support both party and account ledgers.

**Please deploy the backend changes manually** by running the following command in your terminal from the `c:\app\accnad-app\functions` directory:

```bash
npm run deploy
```

I will now proceed with the frontend changes.